backend:
  name: git-gateway
  branch: main

media_folder: assets
public_folder: assets

collections:

  # ── SITE SETTINGS ──────────────────────────────────────────────────────
  - name: settings
    label: "⚙️ Site Settings"
    files:
      - name: site
        label: "Contact Info & Global Settings"
        file: content/site.json
        format: json
        fields:
          - { label: "Company Name", name: company_name, widget: string }
          - { label: "Email Address", name: email, widget: string }
          - { label: "Phone Number", name: phone, widget: string }
          - { label: "Footer Tagline", name: footer_tagline, widget: string }
          - { label: "Bottom Tagline", name: tagline, widget: string }

  # ── HOME PAGE ──────────────────────────────────────────────────────────
  - name: home
    label: "🏠 Home Page"
    files:
      - name: home
        label: "Home Page Content"
        file: content/home.json
        format: json
        fields:
          - widget: object
            name: hero
            label: "Hero Section"
            fields:
              - { label: "Eyebrow Text", name: hero_eyebrow, widget: string }
              - { label: "Main Title", name: hero_title, widget: text, hint: "Use \\n for line breaks" }
              - { label: "Subtitle", name: hero_subtitle, widget: text }
              - { label: "Primary Button", name: hero_cta_primary, widget: string }
              - { label: "Secondary Button", name: hero_cta_secondary, widget: string }

          - widget: object
            name: what_we_do
            label: "What We Do Section"
            fields:
              - { label: "Section Label", name: what_we_do_label, widget: string }
              - { label: "Title", name: what_we_do_title, widget: string }
              - { label: "Body Text", name: what_we_do_text, widget: text }
              - label: "Capabilities List"
                name: what_we_do_capabilities
                widget: list
                field: { label: "Item", name: item, widget: string }

          - widget: object
            name: about
            label: "About Section"
            fields:
              - { label: "Section Label", name: about_label, widget: string }
              - { label: "Title", name: about_title, widget: string }
              - { label: "Paragraph 1", name: about_text1, widget: text }
              - { label: "Paragraph 2", name: about_text2, widget: text }

          - widget: object
            name: cta
            label: "Call to Action Section"
            fields:
              - { label: "Title", name: cta_title, widget: string }
              - { label: "Subtitle", name: cta_subtitle, widget: string }
              - { label: "Primary Button", name: cta_primary, widget: string }
              - { label: "Secondary Button", name: cta_secondary, widget: string }

          - label: "Industries Served"
            name: industries
            widget: list
            field: { label: "Industry", name: industry, widget: string }

  # ── SERVICES PAGE ──────────────────────────────────────────────────────
  - name: services
    label: "🔧 Services Page"
    files:
      - name: services
        label: "Services Content"
        file: content/services.json
        format: json
        fields:
          - { label: "Page Label", name: page_label, widget: string }
          - { label: "Page Title", name: page_title, widget: string }
          - label: "Services"
            name: services
            widget: list
            fields:
              - { label: "Service ID", name: id, widget: string, hint: "Don't change this" }
              - { label: "Number", name: number, widget: string }
              - { label: "Label", name: label, widget: string }
              - { label: "Title", name: title, widget: string }
              - { label: "Description", name: description, widget: text }
              - label: "Specs"
                name: specs
                widget: list
                fields:
                  - { label: "Label", name: label, widget: string }
                  - { label: "Value", name: value, widget: string }
              - { label: "Image", name: image, widget: image }

  # ── GALLERY PAGE ───────────────────────────────────────────────────────
  - name: gallery
    label: "🖼️ Gallery"
    files:
      - name: gallery
        label: "Gallery Photos"
        file: content/gallery.json
        format: json
        fields:
          - { label: "Page Label", name: page_label, widget: string }
          - { label: "Page Title", name: page_title, widget: string }
          - { label: "Page Description", name: page_description, widget: text }
          - label: "Gallery Items"
            name: items
            widget: list
            fields:
              - { label: "Photo", name: image, widget: image }
              - { label: "Caption", name: label, widget: string }

  # ── CONTACT PAGE ───────────────────────────────────────────────────────
  - name: contact
    label: "📞 Contact Page"
    files:
      - name: contact
        label: "Contact Page Content"
        file: content/contact.json
        format: json
        fields:
          - { label: "Page Title", name: page_title, widget: string }
          - { label: "Page Description", name: page_description, widget: text }
          - { label: "Section Title", name: section_title, widget: string }
          - { label: "Section Text", name: section_text, widget: text }
          - { label: "Email", name: email, widget: string }
          - { label: "Phone", name: phone, widget: string }
          - { label: "Response Time", name: response_time, widget: string }
          - label: "Industries Served"
            name: industries
            widget: list
            field: { label: "Industry", name: industry, widget: string }
